# Additional Documentation

This folder holds documentation that doesn't belong in the top-level
`README.md` or `INSTALLATION.md` (e.g. architecture decision records,
API contracts, module specs) as the project grows module by module.
