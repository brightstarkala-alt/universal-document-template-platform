# Auto-loaded by Vite/Vitest when running in "test" mode.
# Provides safe dummy values so the test suite never depends on a real
# developer .env file being present (e.g. in CI).
VITE_API_BASE_URL=http://localhost:8000
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
